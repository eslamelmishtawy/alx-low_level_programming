#include <stdlib.h>
/**
 * malloc_checked - main
 * @b: s
 *
 * Return: absolute value of @n
 */
void *malloc_checked(unsigned int b)
{
	ar = malloc(b);
	if (ar == NULL)
	{
		return (98);
	}
	return (ar)
}
