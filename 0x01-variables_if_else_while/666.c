#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int i;
        int j;
        int k;
        int l;

        i = (50 - 48) + ((56-48) * 10);
        printf("%d",i);
        return (0);
}
