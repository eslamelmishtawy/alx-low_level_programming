#include "main.h"
#include <stdio.h>
/**
 * _strcat - reset value on n to 98
 * @a: pointer to value of n
 * @b: c
 *
 * Retrun: return n
 */
char *_strcat(char *a, char *b)
{
	return strcat(a, b);
}
