#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * factorial - check the code
 * @s: s
 *
 * Return: Always 0.
 */
int factorial(char *s)
{
	if (n < 0)
	{
		return -1;
	}
	if (n == 0)
	{
		return (1);
	}
	return (n * factorial(n - 1)) ;
}
