#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

/**
 * add - prints all alphabets.
 * @a: first number
 * @b: second number
 * Return: always 0
 */
int add(int a, int b)
{
	return (a + b);
}
