#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isupper - prints all alphabets.
 * @c: char under test
 * Return: always 0
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
