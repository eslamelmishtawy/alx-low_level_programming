#include <stdio.h>
/**
 * main - main
 * @str: argc
 * 
 * Return: absolute value of @n
 */
char *_strdup(char *str)
{
	char *ar;

	if (*str == NULL)
	{
		return (NULL);
	}
	ar = malloc(n * sizeof(char));

	return (ar);
}
